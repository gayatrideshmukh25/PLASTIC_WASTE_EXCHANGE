<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login / Signup - Plastic Waste Exchange</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link href="/style.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css">
    <style>
        body {
            margin: 0;
            background: linear-gradient(135deg, #a18cd1 0%, #fbc2eb 100%);
            font-family: 'Inter', sans-serif;
            min-height: 100vh;
        }
        .authcontainer {
            background: #fff;
            border-radius: 24px;
            box-shadow: 0 8px 32px rgba(161,140,209,0.10);
            padding: 2.5rem 2rem 2rem 2rem;
            max-width: 370px;
            width: 90vw;
            margin: 4rem auto 0 auto;
            text-align: center;
            animation: fadeIn 0.7s;
        }
        .authcontainer h2 {
            color: #7b2ff2;
            margin-bottom: 2rem;
            font-weight: 700;
            font-size: 2rem;
        }
        .authcontainer input[type="email"],
        .authcontainer input[type="password"] {
            width: 90%;
            padding: 0.9rem 1rem;
            margin-bottom: 1.2rem;
            border: 1.5px solid #e0d7f7;
            border-radius: 12px;
            font-size: 1rem;
            background: #f3e8ff;
            color: #5e548e;
            transition: border 0.2s;
        }
        .authcontainer input:focus {
            border: 1.5px solid #7b2ff2;
            outline: none;
            background: #fff;
        }
        .submit-btn {
            width: 100%;
            padding: 0.9rem 0;
            background: #7b2ff2;
            color: #fff;
            border: none;
            border-radius: 25px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            margin-top: 0.5rem;
            box-shadow: 0 2px 8px rgba(123,47,242,0.08);
            transition: background 0.2s, box-shadow 0.2s;
        }
        .submit-btn:hover {
            background: #ff61a6;
            box-shadow: 0 4px 16px rgba(255,97,166,0.16);
        }
        .form-footer {
            margin-top: 1.5rem;
            color: #5e548e;
            font-size: 1rem;
        }
        .form-footer a {
            color: #ff61a6;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.2s;
        }
        .form-footer a:hover {
            color: #7b2ff2;
            text-decoration: underline;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-30px);}
            to { opacity: 1; transform: translateY(0);}
        }
        /* Navbar styles */
        header {
            background: #fff;
            box-shadow: 0 2px 8px rgba(161,140,209,0.08);
            padding: 1.2rem 0;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: sticky;
            top: 0;
            z-index: 10;
        }
        header h1 {
            display: flex;
            align-items: center;
            font-size: 1.5rem;
            font-weight: 700;
            color: #7b2ff2;
            margin-left: 2rem;
            letter-spacing: 1px;
        }
        header img {
            margin-right: 1rem;
        }
        nav {
            margin-right: 2rem;
        }
        nav a {
            color: #5e548e;
            text-decoration: none;
            margin: 0 1.2rem;
            font-weight: 500;
            transition: color 0.2s;
            position: relative;
        }
        nav a::after {
            content: '';
            display: block;
            width: 0;
            height: 2px;
            background: #7b2ff2;
            transition: width .3s;
            position: absolute;
            bottom: -4px;
            left: 0;
        }
        nav a:hover {
            color: #7b2ff2;
        }
        nav a:hover::after {
            width: 100%;
        }
        @media (max-width: 600px) {
            .authcontainer {
                padding: 1.2rem 0.5rem;
            }
            header h1 {
                font-size: 1rem;
                margin-left: 0.5rem;
            }
            nav {
                margin-right: 0.5rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>
            <img src="/images/image.png" alt="Recycle" width="38"/>
            PLASTIC WASTE EXCHANGE
        </h1>
        <nav>
            <a href="/">Home</a>
            <a href="/about">About</a>
            <a href="/contact">Contact</a>
            <a href="/signup" class="login-btn">Signup</a>
        </nav>
    </header>
    <div class="authcontainer">
       <h2>Login</h2>
        <!-- Login Form -->
        <form id="loginForm" class="active" action="/postlogin" method="post">
            <input type="email" name="email" placeholder="Email" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit" class="submit-btn">Login</button>
        </form>
        <div class="form-footer">
            Don't have an account? <a href="/signup" id="toSignup">Signup</a>
        </div>
    </div>
</body>
</html>
